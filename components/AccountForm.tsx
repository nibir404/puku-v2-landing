'use client';
import {useState} from 'react';
import {ArrowRight,Mail,Github} from 'lucide-react';
export default function AccountForm({signup=false}:{signup?:boolean}){const[message,setMessage]=useState('');return <main id="main-content" className="account-page"><a href="/"><img src="/puku-logo.png" alt="PUKU" width="44"/></a><h1>{signup?'Start building with PUKU':'Welcome back to PUKU'}</h1><p>Your AI software engineer, ready to work.</p><form onSubmit={e=>{e.preventDefault();setMessage('This is the PUKU website preview. Authentication needs to be connected before you can continue.')}}><label htmlFor="email">Work email</label><input id="email" type="email" placeholder="you@company.com" autoComplete="email" required/><button type="submit">Continue with email <ArrowRight size={18}/></button></form><div className="account-divider">or</div><button className="account-provider" onClick={()=>setMessage('Connect your GitHub OAuth application to enable sign in.')}><Github size={18}/> Continue with GitHub</button><button className="account-provider" onClick={()=>setMessage('Connect your organization’s identity provider to enable SSO.')}><Mail size={18}/> Continue with SSO</button>{message&&<p className="account-status" role="status">{message}</p>}<p className="account-switch">{signup?'Already have an account?':'New to PUKU?'} <a href={signup?'/login':'/signup'}>{signup?'Log in':'Sign up'}</a></p><a className="account-back" href="/">← Back to PUKU</a></main>}
